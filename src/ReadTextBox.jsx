import React from "react";
import "./App.css"; // Import the CSS file for styling

function ReadTextBox({ text }) {
  return <div className="read-text-box">{text}</div>;
}

export default ReadTextBox;
